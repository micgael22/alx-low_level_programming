#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * followed by a new line, except q and e
 * Return: Always 0
 */

int main(void)
{
	char c = '0';

       while (c <= '0')
	{
		puthchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
